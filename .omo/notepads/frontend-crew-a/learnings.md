# Learnings — frontend-crew-a

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /start-work. Append new entries below - never overwrite._

---

## 2026-09-01 — TASK-04 PWA shell + Tailwind+DaisyUI fix
- Manifest theme: `#0F7A4A` primary 7.1:1 AAA, daisyUI light theme primary `#0F7A4A` via `tailwind.config.js` daisyui.themes.light.primary
- Manifest spec verbatim: `VitePWA({ registerType:'autoUpdate', manifest:{ name:'Inventaris AI Tebus Murah', short_name:'TebusMurah', theme_color:'#0F7A4A', background_color:'#FFFFFF', display:'standalone', scope:'/', start_url:'/', icons:[192,512,maskable] }, workbox:{ globPatterns:['**/*.{js,css,html,ico,png,svg,woff2}'], clientsClaim:true, skipWaiting:true } })` — workbox clientsClaim/skipWaiting verified in dist/sw.js
- Icons: `public/icons/pwa-192x192.png` (15,122,74 solid), `pwa-512x512.png`, `pwa-512x512-maskable.png` distinct darker #0B5C38 for maskable entry — png 413/1496/1497 bytes via python zlib
- Hook API: `src/hooks/usePWAInstall.ts` — listens `beforeinstallprompt`, respects `localStorage pwa-prompt-dismissed-at` 7 days (DISMISS_DAYS=7, 7*24*60*60*1000), exposes `{canInstall,promptInstall,dismiss,isDismissed}`, handles appinstalled + standalone check
- Fallback: `src/components/OfflineFallback.tsx` uses DaisyUI `btn btn-primary min-h-[48px] h-12 w-full max-w-[360px]` + iconoir-react WifiOff/RefreshDouble, message "Kamu offline, data tersimpan lokal akan tampil saat ada", 48px full-width verified via playwright boundingBox>200 and haveCSS min-height 48px
- InstallPrompt: `src/components/InstallPrompt.tsx` daisyUI `btn btn-primary flex-1 min-h-[48px]` for Pasang, btn ghost for Nanti, Download iconoir-react
- Tailwind+DaisyUI wiring: `tailwind.config.js` content `["./index.html","./src/**/*.{js,ts,jsx,tsx}"]` + plugin `require("daisyui")` + theme light primary #0F7A4A; `postcss.config.js` tailwindcss+autoprefixer; `src/index.css` replaced with `@tailwind base/components/utilities` + @layer base body `bg-[#F5F5F0]` + btn-primary override
- Build fix: pretty-manifest plugin in `vite.config.ts` writes `dist/manifest.webmanifest` as `JSON.stringify(j,null,2)` so `grep '"theme_color": "#0F7A4A"'` passes (vite-plugin-pwa emits minified without space)
- Verification: `bun install && bun run build` exit 0, `test -f dist/manifest.webmanifest && test -f dist/sw.js && grep '"theme_color": "#0F7A4A"'`, `npx playwright test e2e/pwa.spec.ts` 6/6 passed, `grep daisyui tailwind.config.js && grep @tailwind src/index.css && grep tailwindcss package.json` all PASS, `grep -r "from.*dexie" src/components` 0

## 2026-09-01 — TASK-15 Approve Dialog + PromoAktifList lifecycle (Crew A isolated)
- Files: `src/features/promo/promo.types.ts` (Promo type + createDemoPromos fixture HPP*0.85=8500 floor view-only, Modal vs HPP wording per oracle Q9), `src/features/promo/ApproveDialog.tsx` (DaisyUI modal `role=dialog aria-modal=true` focus trap Tab+Escape, body scroll lock, shadow-dialog `0 8px 24px`, buttons Full width `min-h-[48px] w-full btn-primary #0F7A4A` pressed #0B5C38, guardrail view `title="HPP*0.85=8.500"` + pesan `Harga tebus tidak boleh di bawah HPP x 0.85`, Bahasa Formal `Yakin setujui tebus murah Susu UHT 1L + Roti Tawar harga 9.000?`), `src/features/promo/PromoAktifList.tsx` (cards `shadow-card 0 2px 8px` + Modal `Rp10.000 14px #595959` + Tebus `Rp9.000 18px/600 #0F7A4A` + caption `Untung tipis Rp500` + title floor, pasangan Roti Tawar + alasan AI, status badge Diulas → Aktif, proposed list + active list `data-testid` promo-card-proposed/active, offline banner kuning `bg #FFF8E1 border #F9A825 role=status` `Kamu offline, saran kemarin tetap tampil` via `?offline=1` / `?stale=1` / `navigator.onLine`, toast `role=status aria-live=polite` `success-bg #E8F5E9` `Tebus murah aktif, tampil di Dashboard` `transform translateY 200ms GPU` + dismiss X `Xmark` 4s auto + willChange, `iconoir-react` Xmark/CheckCircle/WarningCircle/Shop/Timer — no lucide, no dexie import, no harga_tebus calc in UI)
- App wiring: `src/App.tsx` replaced static Promo Aktif placeholder with `<PromoAktifList />` (preserves `max-w-480px px-4`, UrgentList + Promo stack), supports URL params `promo=active|guardrailFail|empty` for e2e seeding, window injection `__PROMO_INITIAL__` / `__OFFLINE_STALE__`
- Verification MCP (NOT npx): `bun run build` exit 0 (185kB gzip 56kB), `grep -r "from.*dexie" src/components src/features` 0, `grep -r supabase|firebase|ocr|qrcode` 0, `playwright_browser_navigate` http://127.0.0.1:4174 → cards show Modal Rp10.000 + Tebus Rp9.000 18px/600 + guardrail Untung tipis, `playwright_browser_click` Setujui → Dialog `role=dialog` appears `Yakin setujui... harga 9.000?`, `evaluate` btn `minHeight 48px`, dialog `aria-modal true`, shadow-card `0 2px 8px` + shadow-dialog `0 8px 24px`, `click` Yakin → promo moves to `Promo Aktif (1)` with `badge-aktif-hijau` + toast `bg rgb(232,245,233) #E8F5E9 role=status aria-live=polite` 4s + dismiss X works (opacity → removed), `?offline=1` + `?stale=1` banner `bg rgb(255,248,225) exists`, `?promo=guardrailFail` dialog disabled `Di bawah floor Rp8.500` + alert found, 3-tap KPI: pilih kartu 1 → Setujui 2 → Yakin 3 = 3 taps lolos
- Guardrails: Crew A only promo UI, no `src/db/**` `src/engine/**` `src/advisor/**` `src/lib/validation.ts`, floor view-only via title attr, no POS auto, iconoir-react Q15, Formal warung `Setujui Tebus Murah` (Q20), Branch `feat/TASK-15-approve-dialog` isolated
- Pitfall fix: `iconoir-react` has no `Cancel` export → use `Xmark`; need `vite preview` 4174 (4173 busy) for MCP; toast not in snapshot — must `evaluate` with timeout 300-500ms; `pretty-manifest` already in vite.config for theme_color grep
