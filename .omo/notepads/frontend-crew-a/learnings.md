# Learnings — frontend-crew-a

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /start-work. Append new entries below - never overwrite._

---

## 2026-09-01 — TASK-04 PWA shell + Tailwind+DaisyUI fix
- Manifest theme: `#0F7A4A` primary 7.1:1 AAA, daisyUI light theme primary `#0F7A4A` via `tailwind.config.js` daisyui.themes.light.primary
- Manifest spec verbatim: `VitePWA({ registerType:'autoUpdate', manifest:{ name:'Inventaris AI Tebus Murah', short_name:'TebusMurah', theme_color:'#0F7A4A', background_color:'#FFFFFF', display:'standalone', scope:'/', start_url:'/', icons:[192,512,maskable] }, workbox:{ globPatterns:['**/*.{js,css,html,ico,png,svg,woff2}'], clientsClaim:true, skipWaiting:true } })` — workbox clientsClaim/skipWaiting verified in dist/sw.js
- Icons: `public/icons/pwa-192x192.png` (15,122,74 solid), `pwa-512x512.png`, `pwa-512x512-maskable.png` distinct darker #0B5C38 for maskable entry — png 413/1496/1497 bytes via python zlib
- Hook API: `src/hooks/usePWAInstall.ts` — listens `beforeinstallprompt`, respects `localStorage pwa-prompt-dismissed-at` 7 days (DISMISS_DAYS=7, 7*24*60*60*1000), exposes `{canInstall,promptInstall,dismiss,isDismissed}`, handles appinstalled + standalone check
- Fallback: `src/components/OfflineFallback.tsx` uses DaisyUI `btn btn-primary min-h-[48px] h-12 w-full max-w-[360px]` + iconoir-react WifiOff/RefreshDouble, message "Kamu offline, data tersimpan lokal akan tampil saat ada", 48px full-width verified via playwright boundingBox>200 and haveCSS min-height 48px
- InstallPrompt: `src/components/InstallPrompt.tsx` daisyUI `btn btn-primary flex-1 min-h-[48px]` for Pasang, btn ghost for Nanti, Download iconoir-react
- Tailwind+DaisyUI wiring: `tailwind.config.js` content `["./index.html","./src/**/*.{js,ts,jsx,tsx}"]` + plugin `require("daisyui")` + theme light primary #0F7A4A; `postcss.config.js` tailwindcss+autoprefixer; `src/index.css` replaced with `@tailwind base/components/utilities` + @layer base body `bg-[#F5F5F0]` + btn-primary override
- Build fix: pretty-manifest plugin in `vite.config.ts` writes `dist/manifest.webmanifest` as `JSON.stringify(j,null,2)` so `grep '"theme_color": "#0F7A4A"'` passes (vite-plugin-pwa emits minified without space)
- Verification: `bun install && bun run build` exit 0, `test -f dist/manifest.webmanifest && test -f dist/sw.js && grep '"theme_color": "#0F7A4A"'`, `npx playwright test e2e/pwa.spec.ts` 6/6 passed, `grep daisyui tailwind.config.js && grep @tailwind src/index.css && grep tailwindcss package.json` all PASS, `grep -r "from.*dexie" src/components` 0

## 2026-09-01 — TASK-15 Approve Dialog + PromoAktifList lifecycle (Crew A isolated)
- Files: `src/features/promo/promo.types.ts` (Promo type + createDemoPromos fixture HPP*0.85=8500 floor view-only, Modal vs HPP wording per oracle Q9), `src/features/promo/ApproveDialog.tsx` (DaisyUI modal `role=dialog aria-modal=true` focus trap Tab+Escape, body scroll lock, shadow-dialog `0 8px 24px`, buttons Full width `min-h-[48px] w-full btn-primary #0F7A4A` pressed #0B5C38, guardrail view `title="HPP*0.85=8.500"` + pesan `Harga tebus tidak boleh di bawah HPP x 0.85`, Bahasa Formal `Yakin setujui tebus murah Susu UHT 1L + Roti Tawar harga 9.000?`), `src/features/promo/PromoAktifList.tsx` (cards `shadow-card 0 2px 8px` + Modal `Rp10.000 14px #595959` + Tebus `Rp9.000 18px/600 #0F7A4A` + caption `Untung tipis Rp500` + title floor, pasangan Roti Tawar + alasan AI, status badge Diulas → Aktif, proposed list + active list `data-testid` promo-card-proposed/active, offline banner kuning `bg #FFF8E1 border #F9A825 role=status` `Kamu offline, saran kemarin tetap tampil` via `?offline=1` / `?stale=1` / `navigator.onLine`, toast `role=status aria-live=polite` `success-bg #E8F5E9` `Tebus murah aktif, tampil di Dashboard` `transform translateY 200ms GPU` + dismiss X `Xmark` 4s auto + willChange, `iconoir-react` Xmark/CheckCircle/WarningCircle/Shop/Timer — no lucide, no dexie import, no harga_tebus calc in UI)
- App wiring: `src/App.tsx` replaced static Promo Aktif placeholder with `<PromoAktifList />` (preserves `max-w-480px px-4`, UrgentList + Promo stack), supports URL params `promo=active|guardrailFail|empty` for e2e seeding, window injection `__PROMO_INITIAL__` / `__OFFLINE_STALE__`
- Verification MCP (NOT npx): `bun run build` exit 0 (185kB gzip 56kB), `grep -r "from.*dexie" src/components src/features` 0, `grep -r supabase|firebase|ocr|qrcode` 0, `playwright_browser_navigate` http://127.0.0.1:4174 → cards show Modal Rp10.000 + Tebus Rp9.000 18px/600 + guardrail Untung tipis, `playwright_browser_click` Setujui → Dialog `role=dialog` appears `Yakin setujui... harga 9.000?`, `evaluate` btn `minHeight 48px`, dialog `aria-modal true`, shadow-card `0 2px 8px` + shadow-dialog `0 8px 24px`, `click` Yakin → promo moves to `Promo Aktif (1)` with `badge-aktif-hijau` + toast `bg rgb(232,245,233) #E8F5E9 role=status aria-live=polite` 4s + dismiss X works (opacity → removed), `?offline=1` + `?stale=1` banner `bg rgb(255,248,225) exists`, `?promo=guardrailFail` dialog disabled `Di bawah floor Rp8.500` + alert found, 3-tap KPI: pilih kartu 1 → Setujui 2 → Yakin 3 = 3 taps lolos
- Guardrails: Crew A only promo UI, no `src/db/**` `src/engine/**` `src/advisor/**` `src/lib/validation.ts`, floor view-only via title attr, no POS auto, iconoir-react Q15, Formal warung `Setujui Tebus Murah` (Q20), Branch `feat/TASK-15-approve-dialog` isolated
- Pitfall fix: `iconoir-react` has no `Cancel` export → use `Xmark`; need `vite preview` 4174 (4173 busy) for MCP; toast not in snapshot — must `evaluate` with timeout 300-500ms; `pretty-manifest` already in vite.config for theme_color grep

## 2026-09-02 — TASK-17 Dashboard Promo Aktif + Histori Saran UI (Crew A isolated)
- Files: `src/features/dashboard/DashboardPage.tsx` (3 seksi: Stok Mepet via UrgentList, PromoAktif via PromoAktifList, Histori 5 terbaru via HistoriList — max-w 480px mx-auto, px-4, h-6 spacer 24px token space-lg, data-testid section-urgent/promo/histori + dashboard-page), `src/features/dashboard/HistoriList.tsx` (FakeRepository 10 items histori desc limit 5, empty SVG 48px outline circle 16r + clock hand, not emoji, card p-4 shadow 0 2px 8px, hover border #0F7A4A, pasangan badge #0F7A4A 12px + timestamp 12px Clock 14px, tap → pushState /histori/:id, 5 desc via getHistoriTerbaru, "Menampilkan 5 terbaru dari 10 saran", Muat Ulang 48px btn-outline), `src/features/dashboard/HistoriDetailPage.tsx` (route /histori/:id via useHistoriRoute pushState wrapper, Back btn w-full min-h 48px font 16px border #D9D9D9 NavArrowLeft 18px, card shadow 0 2px 8px, timestamp 12px Clock 14px Asia/Jakarta, pasangan + harga_tebus Rp 12px/16px/600 #0F7A4A, alasan 16px, badge confidence, ID org toko-01, not-found fallback), `src/lib/fakeHistoriRepository.ts` (10 templates aksi/alasan/pasangan harga_tebus confidence, daysAgoISO created_at desc, getHistoriTerbaru sort desc limit 5, demoHistori singleton, no dexie), `src/App.tsx` hist routing (useHistoriRoute regex ^/histori/([^/]+), orig pushState bind + wrapped dispatch popstate, historiId → HistoriDetailPage, else DashboardPage inside main max-w 480 p-16 pb-80, header #0F7A4A + nav 48px, fix TS2683 this implicit any via bind + arrow wrapper)
- Design tokens: primary #0F7A4A, surface #F5F5F0 page, card white border #D9D9D9 radius 12px shadow-card 0 2px 8px, font body 16px timestamp 12px heading 20px/700, btn 48px full width DaisyUI btn-outline/btn-primary, iconoir-react WarningCircle/Clock/NavArrowLeft/Shop/NavArrowRight 14px (detail 18px), no lucide, no dexie leak (grep 0), no supabase leak
- Verification MCP ONLY: `bun run build` exit 0 (201kB gzip 59kB vite 1713 modules), `grep -r "from.*dexie" src/features/dashboard` 0, `playwright_browser_navigate` / → snapshot 3 headings Stok Mepet/Promo Tebus Murah/Histori Saran (Histori 5 cards with pasangan badge 12px + timestamp 12px), `evaluate` main maxWidth 480px padding 16px no hScroll 375px innerWidth 375 scrollWidth 360, iconoir width 14, bodyFonts 16px btn 48px modal Rp10.000 present, shadow-card 0 2px 8px, `click histori hist-1` → /histori/hist-1 detail shows aksi Tebus Murah Susu UHT 1L + Roti Tawar + alasan Susu mau kadaluarsa + pasangan Roti Tawar + harga Rp9.000 + timestamp 12px + Back 48px + cardShadow 0 2px 8px, Back click → / histori returns, `?histori=empty` → empty SVG 48px w/h 48 not emoji, `resize 375x800` hasHScroll false
- Guardrails: Crew A only dashboard, no src/db|engine|advisor|validation, fakeHistoriRepository mock-friendly, build passes, push feat/TASK-17-dashboard-histori PR FRD-05 trace
- Pitfall fix: wt-TASK-17 dist missing due to bun run without cd → must `cd $workdir && bun run build` ; vite preview port 4174 stale cache → pkill + rebuild ; App.tsx this implicit any → use bind + arrow wrapper not function(this)

## 2026-09-02 — TASK-19 Threshold Settings Page + HPP/margin config (Crew A isolated)
- Files: `src/features/settings/SettingsPage.tsx` (3 kategori Dairy/Snack/Beras cards `shadow-card 0 2px 8px` + grid `grid-cols-1 md:grid-cols-3 gap-2` stacked 375px vs 3-col desktop, `DaisyUI input input-bordered min-h-[48px] h-12 text-base w-full [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none` + `inputMode=numeric pattern=[0-9]* min=1` + block `e/E/-/+/./,` via `onKeyDown preventDefault`, `aria-invalid=true` + `border-[#C62828] border-2 input-error` + `aria-describedby error-id` + inline `<p role=alert>` pesan Bahasa Indonesia `Threshold tidak boleh kosong / Angka tidak boleh sama / Harus urut besar ke kecil / Threshold harus lebih dari 0` 14px #C62828 + `WarningCircle 14px`, floor view read-only `Rp 8.500 (HPP Rp 10.000 ×0.85)` via `floorHarga = Math.floor(hpp*0.85)` + `formatRupiah id-ID` per kategori Dairy 10000→8500 Snack 8000→6800 Beras 12000→10200 in `bg #F5F5F0 border #D9D9D9 rounded 8px`, `btn btn-primary w-full min-h-[48px] h-12 font-semibold #0F7A4A` Simpan per kategori, toast `role=status aria-live=polite` DaisyUI `alert alert-success bg-[#E8F5E9] border #0F7A4A` / `alert-error bg-[#FFEBEE] border #C62828` 4s auto with `CheckCircle/WarningCircle 18px`, `iconoir-react Settings/CheckCircle/WarningCircle/InfoCircle` — no lucide, no dexie leak), `src/lib/settingsStore.ts` (mock `KategoriSetting id=nama threshold_h_minus org_id contoh_hpp` defaults `[7,3,1]` per Kategori, `STORAGE_KEY settings-threshold-store-v1` localStorage + memoryCache, `validateThreshold` descending >0 no dup empty, `getKategoris/setKategoris/updateThreshold/resetToDefaults` + `formatRupiah/floorHarga`, `window.__SETTINGS_STORE__` expose for e2e, no dexie import), `src/App.tsx` hist+settings routing (`useRoute` pushState/replaceState wrap `...unknown[] => void` fix TS2556 spread never, `route==="/settings"` → `<SettingsPage/>` else dashboard, header #0F7A4A + main `max-w 480 px-4 pb-80`, `nav 5 item` Dashboard `Home` / SKU `Box` / Batch `Package` / Promo `ShoppingBag` / Pengaturan `Settings` 20px `min-h 48px` flex column gap 2 `data-testid nav-pengaturan` + `aria-current page` + pushState, 16px font)
- Design tokens: primary #0F7A4A, surface #F5F5F0 page, card white border #D9D9D9 radius 12px shadow 0 2px 8px, font body 16px heading 24px/700, btn/input 48px DaisyUI form-control, iconoir 14/18/20px, contrast AA, Bahasa Indonesia labels `Pengaturan / Ubah angka H- / Nilai 1 Terbesar`
- Verification MCP ONLY: `bun run build` exit 0 (203kB gzip 60kB 1711 modules), `grep -r "from.*dexie" src/features/settings` 0, `grep supabase|firebase|ocr|qrcode` 0, `playwright_browser_navigate` /settings → snapshot Pengaturan h1 + 3 kategori cards [7,3,1] each + floor Rp 8.500/6.800/10.200, `evaluate` inputCount 9 type number inputMode numeric grid `grid-cols-1 md:grid-cols-3` btn minHeight 48px nav 5 Pengaturan, floor visible, font 16px, `setReactValue` Dairy 14,7,3 → save → `[14,7,3]` + localStorage `settings-threshold-store-v1` contains Dairy [14,7,3] + toast `role=status alert-success` 4s `Threshold Dairy disimpan: [14,7,3]`, reload persist [14,7,3], invalid Snack [3,3,1] → `error-Snack Angka tidak boleh sama` + `border rgb(198,40,40) #C62828 border 2px input-error aria-invalid true` + toast error, descending [1,7,3] → `Harus urut besar ke kecil`, empty Beras → `Threshold tidak boleh kosong` aria-invalid, block e/E/- via `keydown preventDefault` + `appearance:textfield`, `resize 375x800` no hScroll input widths 262 (stacked) vs 1280 3-col, `nav Pengaturan click` → /settings pushState + `Dashboard click` → / returns
- Guardrails: Crew A only settings, no src/db|engine|advisor, settingsStore mock-friendly, build passes, push feat/TASK-19-settings-threshold PR FRD-05 trace
- Pitfall fix: React controlled input set via native setter `Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype,'value').set.call(el,val)` + `dispatchEvent input` not just `el.value=`, otherwise state not updated; TS spread `Parameters<typeof pushState>` fails `never` iterator → use `...unknown[]`; vite preview port 4174 serves wt-TASK-19 dist `index-CKf6yziU.js` not UNEJ, need `pkill -9` not `pkill`; toast not in snapshot → `evaluate` with 300ms delay; grid `md:grid-cols-3` not `grid-cols-3` for stacked mobile
