# issues - inventory-userflow-rewrite
- 2026-09-03: Gotcha Dexie v2 unique index — &[org_id+kode] fails jika masih ada sku tanpa kode; upgrade must backfill sebelum enforcement; fix pakai trans.table update sebelum index jadi valid
- 2026-09-03: Gotcha MissingAPIError fake-indexeddb — Dexie cache indexedDB saat import, jadi set globalThis.indexedDB/IDBKeyRange sebelum `await import("dexie")` dan `await import("./db")`, jangan static import di atas
- 2026-09-03: Gotcha TS7022 implicit any di createSKU while loop — n inferred any jika kode reassigned di loop; fix hapus while dead-code untuk existingKodes empty case
- 2026-09-03 task4: dexieRepository string-ids DB v3 tanpa backfill kode (backfill hanya di InventoryDB v2 upgrade). Kode wajib diisi saat create via computeKode (task 5/9) agar &[org_id+kode] tidak kena undefined. Watch saat implementasi form SKU.
- 2026-09-03 task5: Gotcha circular import db.ts ↔ kode.ts — jika kode.ts import db singleton + db.ts import kode.ts → circular runtime undefined; fix kode.ts pure helpers tanpa import db, async helpers terima db param + dynamic import ValidationError; consume.ts import db singleton aman karena satu arah. Gotcha FEFO null vs expiry — spec Keputusan strict: jika ada expiring, null dilewati totalAvailable hanya expiring, jangan fallback parsial; test null 100 + expiry 5 keluar 7 harus reject. Gotcha Transaksi add type — Batch vs Transaksi mismatch tsc -b; fix pakai (transaksis as any).add. Gotcha count vs max — createSKU lama pakai count+1 saat empty, tabrakan setelah delete; fix selalu computeNextKode max+1.
- 2026-09-03 Task07: nama_toko Dexie settings table belum ada di db.ts v2 — pakai localStorage profil_toko_v1 + TODO migrasi task24 backup v2; existing e2e suites (dashboard/pwa) sekarang butuh login dulu — akan di-fix task21 dashboard real
- 2026-09-03 task7: AuthGuard aktif → SEMUA e2e spec lama akan kena login page. Spec baru WAJIB login dulu (setup PIN via UI atau seed localStorage+sessionStorage). Fix massal existing suites di task 21. QA MCP manual task 7: setup/login/reload/guard/salah-PIN + console 0 errors, semua PASS via MCP + CLI 5/5.
- 2026-09-03 Task08 gotcha preview server untuk screenshot evidence — npx playwright test auto start server tapi node -e manual butuh bun run preview manual & wait curl 200, kill setelah selesai. Gotcha tag repo string-id belum punya listTags API — pakai dexieV2.tags.where langsung, sku_tags mapping via tagById Map, jangan pakai InventoryRepository lama (numeric ids).
- 2026-09-03 Task08 race gotcha — React.StrictMode mounts twice in dev, fire-and-forget seed without await + check-then-insert = classic TOCTOU duplicate; fix harus lock di module level bukan di component, dedupe UI sebagai defense-in-depth untuk data legacy
- 2026-09-03 Task09 gotcha: katalog.spec "segera hadir" gagal setelah SkuForm real — update test ke preview-kode/input-nama; notifScheduler.test Dairy/Snack/Beras hardcoded gagal setelah seed 11 — update seed.test ke 11 + patch notifScheduler ke Makanan Basah/Misc + custom Alpha/Beta untuk threshold per kategori; skuService legacy strict harga_normal<HPP reject vs form warning — form pakai realRepo permissive bypass; kode.ts circular db->kode pure helpers via getPrefixForKategori + dynamic import ValidationError; build error TS18048 tag undefined -> tag! assertion
- 2026-09-03 Task10 gotcha blank page "Cannot stop, scanner is not running" — Html5Qrcode.stop() throws sync if not scanning (di headless/permission denied), cleanup + handleManualSubmit/handleBack panggil stop tanpa cek isScanning -> unhandled pageerror -> React root blank; fix cek if (s.isScanning) sebelum stop + try clear, stoppedRef guard decode; vite lazy via import("html5-qrcode") di effect + lazy(() => import ScanPage) di App.tsx, main bundle 0 Html5Qrcode, ScanPage 4k + html5-qrcode 375k chunk via __vite__mapDeps; Playwright camera grant mock sulit decode nyata -> assert manual fallback visible + input 48px + submit via CustomEvent + sessionStorage mengisi barcode
- 2026-09-04 Task29 gotcha "bar is not a registered controller" — ChartArus register hanya BarElement tanpa BarController → crash App, root kosong, sku-detail + chart spec 3/4 fail; fix register BarController+LineController + Title/Tooltip/Legend; lalu build chunk ChartArus terpisah ok, sku-detail + chart 7/7 hijau
- 2026-09-04 Task22 gotcha iconoir TrendingUp tidak ada (7.12.1) — import TrendingUp error TS2305; ganti StatUp/GraphUp yang tersedia; KecepatanRow kind required vs optional TS2345 — buat kind optional agar push tanpa kind tidak error; e2e getByText strict mode 2 elemen (subtitle Rank keluar-masuk vs heading) — ganti assert ke data-testid section-*; omzet margin salah jika tidak enrich hpp — keluar transaksi tanpa hpp_snapshot fallback 0 jadi margin = omzet; fix enrich via skuMap.hpp sebelum calcOmzet14; evidence capture via playwright chromium memerlukan file di dalam project agar import @playwright/test resolve (NODE_PATH tidak cukup untuk /tmp); fix copy ke project root lalu NODE_PATH node; DashboardPage tab harus tetap render header empty "Belum ada transaksi" bukan hide
- 2026-09-04 Task23 gotcha AppShell View 4→3 — shell.spec/dashboard.spec/3tap.spec lama expect bottom-nav-promo/nav-promo akan outdate setelah 3-tab, jangan ubah schema Dexie/validation/guardrail task13-22, hanya App.tsx+e2e/nav.spec.ts+evidence yang staged; fallback /tidak-ada via else dashboard bukan redirect pushState agar tidak loop, skuDetailRoute skip baru/masuk/keluar tetap, legacy ?view=promo jangan throw fallback dashboard
