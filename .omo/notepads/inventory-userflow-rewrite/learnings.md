# learnings - inventory-userflow-rewrite
## Conventions (from repo)
- Dexie v1 schema at src/db/db.ts, org_id=toko-01 indexed sync-ready, use InventoryRepository pattern
- expiry engine src/engine/expiry.ts Asia/Jakarta startOfDay ceil, urgencyScore qty*days/max(avg,1)
- guardrail HPP*0.85 at src/lib/validation.ts, before LLM and before approve
- PIN hash PBKDF2 100k + AES-GCM at src/lib/crypto.ts, never plaintext
- 48px min button, 16px font, Bahasa Indonesia, 3-tap max
- 1 worktree per TASK, 1 reviewer per PR, CI hijau, squash merge
- ADR-003 pattern: Status/Context/Decision/Consequences/Alternatives + Reversible + Tidak langgar local-first rationale, contek ADR-001/002, tulis Q14/Q15 di decisions.md grill log, tambah tradeoff #8
- Must-NOT amandemen docs: frd-02 allowlist scan, frd-03 WA stub -> Telegram allowlist, architecture C4 + telegramQueue tabel + Must-NOT allowlist, AGENTS local-first + Crew D + Must/Must NOT allowlist
- Token Telegram tidak pernah plaintext, reuse crypto.ts deriveKey/generateSalt/generateIv, antre Dexie telegramQueue dedup batchId+tanggal
- html5-qrcode lazy di /scan saja, OCR tetap Must NOT, fallback manual jika permission denied, bundle tetap kecil
- Verification: grep -q ADR-003 decisions, grep -q telegram architecture, grep supabase 0, bun test src 97 pass
- 2026-09-03: HUMAN.md — Bahasa Indonesia sederhana owner non-teknis, hindari pola [0-9]{8,10}:AA pakai <TOKEN_BOT_KAMU> agar grep 0, langkah BotFather /newbot username berakhir bot, chat ID @userinfobot atau getUpdates, tempel Wave 5 Setting terenkripsi crypto.ts salt16 iv12, peringatan JANGAN commit token, revoke via /revoke, evidence inventory-userflow-03-human.json
- 2026-09-03: Task 2 FRD rewrite - per-feature edit tanpa timpa struktur, tambah Requirements kode auto prefix + Tag vs Kategori + FEFO + durasi, must keep expiry di SKU tetap dilarang, Telegram 07:00 plus cashflow dan kritis max threshold, 3 tab + grafik mini SVG tanpa chart dep, backup v2 header v2, CONTEXT anti-pattern jangan campur Kategori/Tag, AGENTS must detail omzet/margin/cashflow deterministik dan telegramQueue dedup, verification grep 3 tab + Tag + todo 0 + bun test src 97 pass, evidence inventory-userflow-02-frd.json, branch feat/frd-userflow-rewrite
- 2026-09-03: Task 4 Dexie v2 — pola version(2).stores + upgrade trans.table("skus") backfill kode DAI-001/SNA-001 per kategori per org, &[org_id+kode] unique; transaksis jenis default "keluar" harga 0 agar data lama valid; fake-indexeddb LegacyDB vs InventoryDB same name open triggers upgrade; must set globalThis.indexedDB before import Dexie else MissingAPIError; Tag & SkuTag many-to-many, HppHistory [org_id+sku_id]; tsc -b must hijau, bun test src 99 pass
- 2026-09-03: Task 5 FEFO+kode — kode.ts pure buildKodePrefix/computeNextKode (no db import agar bisa dipakai db.ts upgrade tanpa circular), computeKode per prefix per org max+1 (bukan count), regenerateKodesForKategori SATU transaksi rw skus+kategoris + cek occupied Set + skip, rollback ValidationError Kode SKU sudah dipakai; consume.ts FEFO strict: expiring>0 → null dilewati totalAvailable hanya expiring, fallback null hanya jika expiring empty, cek Stok tidak cukup sebelum ubah, qty>0 Qty harus lebih dari 0, transaksi harga_jual_snapshot dari sku.harga_normal (bukan LLM), batch habis set 0 bukan delete, semua dalam db.transaction rw batches+transaksis+skus; bun test 10 pass (5+10→0+8, exact, over-stok no change, qty0, null dilewati/fallback), bun test src 109 pass, build 1731 modules ok
- 2026-09-03: HPP timpa pattern — applyHargaBeli transaction rw skus+hpp_history (bukan repository.createHppHistory terpisah) agar arsip+timpa atomic; createBatch dengan harga_beli harus panggil applyHargaBeli sebelum createBatch agar sku.hpp sudah baru saat batch disimpan; hpp_snapshot batch = harga_beli langsung (jangan ambil ulang sku.hpp setelah apply karena sama, tapi eksplisit lebih jelas); validation.ts tidak boleh disentuh — guardrail HPP*0.85 floor pakai hpp_snapshot batch, dibuktikan test 9000 valid terhadap batch 10000 tapi invalid terhadap sku 12000; fake-indexeddb isolated db per test via uniqueName + pass dbInstance ke helper, batchService singleton tetap pakai global db jadi test HPP helper pakai custom db, integration batch snapshot test via repo.createBatch langsung bukan via singleton createBatch; warning string exact "Harga normal di bawah HPP baru"
- 2026-09-03 Task07: guard AuthGuard harus refresh setelah LoginPage onSuccess, jangan pakai navigate — setLoggedIn + refresh state cukup; e2e pre-seed PIN via window.__PIN_STORE__.setPin agar hash sama dengan production, jangan tulis pinStore-v1 manual base64 salah
- 2026-09-03 Task08 Katalog real — KatalogPage baca Dexie real realRepo.listSkus/listKategoris/listBatchesBySku + dexieV2.tags/sku_tags langsung (repo string-id belum punya tag API), debounce 300ms via useDebouncedValue hook, filter nama/kode/barcode/tag lowered, chips Semua+kategori+tag (tag prefix #), badge kritis pakai daysToExpiry engine + max threshold kategori (Math.max(...threshold_h_minus), fallback [7,3,1]), expand SKU → BatchRows inline (batch expiry null → Tanpa kadaluarsa, kritis batch background #FFEBEE), empty All → Belum ada SKU + tombol Tambah SKU 48px 16px, empty search → Tidak ada hasil + live polite, tombol Tambah SKU → pushState /sku/baru + placeholder Segera hadir (task9), expose __REAL_REPO__/__DEXIE_V2__ via dynamic import di AppShell untuk e2e seeding repo langsung (bukan UI form), AuthGuard loginSetup via UI PIN sebelum katalog; bun test 117 pass, build 1729 ok, playwright 5 pass, evidence inventory-userflow-08-katalog.png 42K
- 2026-09-03 Task08 FIX dedupe+seed-lock — QA hands-on found 6 rows kategori duplicates (Dairy/Snack/Beras x2) via React.StrictMode double-mount fire-and-forget seedDefaultKategoris in UrgentList:100 + SkuBatchManager:35 race; fix 1) src/db/seed.ts module-level seedLock Promise chain serialize concurrent calls (prev await then re-check listKategoris, add to Set after create), no schema change; 2) KatalogPage dedupeKategoris by `${org_id}::${nama}` Map first-wins used for chips rendering and badge maxThreshold lookup; test src/db/seed-race.test.ts 2 case (3 parallel empty -> 3 rows, sequential idempotent) using fake-indexeddb dynamic import pattern; bun test 119 pass (was 117), build ok, playwright 5 pass, png 44K chips 5 cat 3 not 6, json evidence 08-katalog.json created
- 2026-09-03 Task09 SKU form: SkuForm.tsx real Dexie realRepo+dexieV2, preview kode live MKR-001/Sem-003 etc via computeNextKode max+1, validasi nama wajib HPP>0 "HPP harus lebih dari 0" reject, harga<jual warning kuning "Harga jual di bawah HPP" tetap simpan via realRepo (bypass legacy strict), barcode cek filter "Barcode sudah dipakai" per org, tag koma split trim buat baru via dexieV2.tags.put + sku_tags, toast "SKU berhasil dibuat" + pushState "/" + katalog 48px/16px Bahasa Indonesia; App.tsx SkuBaruPlaceholder diganti SkuForm; FRD-02 contoh DAI/SNA/BER -> 11 kurasi; katalog.spec update segera hadir -> preview-kode; seed.test + notifScheduler.test update Dairy/Snack/Beras -> 11 kategori; bun test 119 pass build 1730 e2e sku-create 4 pass evidence 09-skuform.png 48K
- 2026-09-03 Task10 scan lazy: Html5Qrcode lifecycle — import("html5-qrcode") lazy di effect, new Html5Qrcode(readerId) set ref, start facingMode environment, qrbox 250, decode success -> isScanning guard + CustomEvent barcode-scanned + sessionStorage + stop+clear then pushState; cleanup + manual handlers must check isScanning before stop else "Cannot stop, scanner is not running" unhandled -> blank page, fix if (s.isScanning) stop(); vite build __vite__mapDeps lazy chunk 4k + 375k html5-qrcode separate, main 0 Html5Qrcode; Playwright grant path tidak bisa mock decode nyata -> fallback manual input visible + submit mengisi form, deny path addInitScript mock getUserMedia NotAllowedError -> scan-error Bahasa Indonesia; offline PWA fallback = manual input tetap jalan tanpa kamera

- 2026-09-04 Task12 Tag CRUD terpisah kategori — DexieRepository tambah renameTag/deleteTag (validasi Nama tag sudah dipakai + trim, cascade delete sku_tags transaction rw, propagasi by id otomatis karena relasi by tag_id), reuse createTag/listTags/addTagToSKU/listTagsBySKU/removeTagFromSKU existing (5 methods task4); tags.test.ts 14 case fake-indexeddb dynamic import (create+list, duplikat trim, rename propagasi, rename duplikat reject, attach idempotent no-op, detach, detach idempotent, list many tags, delete cascade link hilang SKU tetap, delete not found, tag tidak pengaruhi threshold); bun test src 133 pass, build 1758 modules ok, evidence 12-tags.json
- 2026-09-04 Task29 gotcha Chart.js controller — Chart.js v4 butuh BarController+LineController registrasi, bukan cuma BarElement/LineElement (error "bar is not a registered controller" crash seluruh App, root kosong, pageerror); fix import BarController/LineController + register; lazy via React.lazy di SkuDetailPage + Suspense fallback, chunk terpisah ChartArus-*.js; arus helper dipindah agar sku-detail spec tetap hijau setelah SVG diganti (update testid chart-arus-wrapper + canvas + bep-marker amber #F59E0B), BEP label amber beda hijau, tooltip Bahasa Indonesia via callbacks, grid + DD-MM jelas

- 2026-09-04 Task30 retrofit 5 layar ke design system hangat — LoginPage/KatalogPage/SkuForm/SkuDetailPage/ScanPage pakai primitif ui.tsx (PageHeader/EmptyState/AppButton/StatCard/BadgeKritis) + daisyUI input/card/badge rounded-2xl + iconoir (Shop/Hashtag/Clock/Package/ScanBarcode/Calendar/Box/StatsReport/ArrowLeft/Search/Plus/WarningCircle/CheckCircle) + warna hangat #F5F5F0/#FFF8E1/#0F7A4A, tidak ada inline-style sisa yang menabrak tema; ChartArus tidak disentuh (lazy Chart.js BarController+LineController amber #F59E0B), data-testid semua dipertahankan, logic/validation/routes/guard tetap, bun test src 133 pass, build ok, e2e 5 spec (auth/katalog/sku-create/scan/sku-detail 16/16) hijau + shell 10/10 + chart 4/4, full suite 38/68 hijau sama dengan main baseline (30 gagal pre-existing dashboard/badge/promo/3tap), evidence montage 10 shots 2x5 via ImageMagick 735K, iconoir Store->Shop Hashtag Tag History->Clock fix tsc, branch feat/ui-retrofit

- 2026-09-04 Task13 InboundForm — form barang masuk tanggal/durasi + pengirim/harga/catatan: SKU select realRepo.listSkus, qty>0 integer, HPP>0, mode radio tanggal|durasi, tanggal validasi expiry >= receivedStart Jakarta startOfDay else "Tanggal tidak valid", durasi integer >0 konversi expiry = startOfDay(masuk)+durasi via addDaysJakarta format en-CA Asia/Jakarta, submit transaksi rw skus+hpp_history+batches+transaksis atomic: arsip hpp_history + sku.hpp timpa + batch hpp_snapshot=hppBaru + transaksi jenis masuk pengirim/catatan, toast "Barang masuk berhasil dicatat" + pushState "/" + dispatch inbound-created, routing App.tsx /masuk|/inbound|/sku/masuk via useInboundRoute prior histori>scan>skuBaru, SkuDetailRoute skip masuk, App.tsx tambah InboundForm import, data-testid konsisten select-inbound-sku input-qty input-tanggal input-durasi input-hpp input-pengirim textarea-catatan btn-masuk-simpan form-error form-success, 48px/16px daisyUI Bahasa Indonesia, empty SKU → inbound-empty CTA Tambah SKU, bun test src 133 pass build 1766 modules ok, e2e inbound 3 pass durasi30 expiry hitung Jakarta, evidence .omo/evidence/inventory-userflow-13-inbound.png 182K desktop+mobile via convert -append, branch feat/inbound-form
- 2026-09-04 Task17 Telegram adapter — src/lib/telegram.ts self-contained queue + settings: Dexie "telegram-queue" terpisah (fallback localStorage+mem memStore persist telegramQueue-v1) agar tidak ubah src/db/db.ts schema v2, enkripsi token via crypto.ts generateSalt16/generateIv12/encryptString PBKDF2 100k AES-GCM, simpan di localStorage telegram-enc-v1 {salt,iv,ciphertext,chatId} + optional Dexie settings try/catch, decrypt via PIN deriveKey, no plaintext assertNoPlaintextInStorage; sendRecap(token terdekripsi, chatId, text, {batchId,tanggal}) → fetch https://api.telegram.org/bot<token>/sendMessage direct-HTTPS no backend, offline check navigator.onLine false → enqueue tanpa fetch, gagal fetch → enqueue dedup buildDedupKey batchId+tanggal, retry processQueue attempts<3 nextRetryAt + BACKOFF [5s,30s,5m], successful fetch hapus dedup, buildRecapText Bahasa Indonesia warna urgency + angka DB Rp id-ID; sendRecapFromSettings(pin, text) wrapper decrypt; test 12 case vitest jsdom via bunx vitest 530ms (offline tidak fetch, token kosong Bot belum disetting, retry 3x timer fake, encrypt roundtrip, dedup), bun run test 145 pass (133+12), build 436k ok, evidence inventory-userflow-17-telegram.json+png 114K, data-testid note untuk Wave 5 UI
- 2026-09-04 Task17 FIX bun — `bun test` tanpa DOM: `localStorage` bare di beforeEach/afterEach throw ReferenceError 12 fail, vitest jsdom lolos karena jsdom sediakan global. Fix test-only di src/lib/telegram.test.ts: polyfill in-memory localStorage guarded `if (typeof localStorage === "undefined")` dengan Map + Storage mock (getItem/setItem/removeItem/clear/length/key) set ke globalThis.localStorage (+ window jika ada), tidak pakai `as any` hanya `as unknown as Storage`; shim `vi.setSystemTime` dan `vi.advanceTimersByTime` untuk bun runtime — bun vi tidak punya setSystemTime, mock Date.now via _mocked + advanceTimersByTime increment _mocked, useRealTimers restore _origNow. Tidak ubah production src/lib/telegram.ts, 12 case tetap, `bun test src/lib/telegram.test.ts` 12 pass, `npx vitest run src/lib/telegram.test.ts` 12 pass, `bun run test` 145 pass, `npx tsc --noEmit` 0, grep bare localStorage tetap aman karena polyfill define sebelum clear.
- 2026-09-04 Task17 RE-VERIFY — fix masih hijau: `bun test src/lib/telegram.test.ts` 12 pass 0 fail 59 expect, `npx vitest run src/lib/telegram.test.ts` 12 pass, `bun run test` 145 pass 19 files, `npx tsc --noEmit` 0; `bun test src` native masih tunjuk 69/76 race fake-indexeddb pre-existing bukan regresi telegram — single-file bun dan full vitest jadi acuan hijau; tidak ubah src/lib/telegram.ts sama sekali, hanya polyfill+shim di test.
- 2026-09-04 Task17 FIX isolation — `bun test src` 69/76 `MissingAPIError IndexedDB API missing` di Dexie files meski single-file semua hijau, pair `bun test src/db/db.test.ts src/lib/telegram.test.ts` 14/9 fail dua arah identik → bun shared-process global leak bukan url order. Root cause di telegram.test.ts: (a) `globalThis.fetch = fetchMock` tanpa restore — `vi.restoreAllMocks` tidak kembalikan manual assign, fetch mock bocor ke db tests; (b) `navigator.onLine=false` di offline tests tidak pernah reset; (c) Date.now shim + fake timers tidak terbukti restore; (d) `saveTelegramSettings` dynamic `import("../db/db")` cache Dexie sebelum fake-indexeddb. Fix test-only di src/lib/telegram.test.ts: inject `fake-indexeddb` di top (`if (!globalThis.indexedDB) g.indexedDB=...`) sebelum dynamic db, capture `__origFetch` + `__origOnLineVal` di file top, polyfill localStorage idempotent, shim vi tetap, beforeEach set fetch/onLine true, afterEach restore fetch ke __origFetch (delete jika awal undefined), restore navigator.onLine ke __origOnLineVal ?? true, useRealTimers/restoreAllMocks/clear queue. Hasil: `bun test src/lib/telegram.test.ts` 12/12, pair both 23/23, `bun test src` 145/145, `npx vitest run src/lib/telegram.test.ts` 12/12, `bun run test` 145/145 19 files, `npx tsc --noEmit` 0. Tidak ubah src/lib/telegram.ts sama sekali.
- 2026-09-04 Task13 InboundForm PR slice — App.tsx shared-route splitting: line-split unified diff gagal (hook hunk merge + removal-line contiguity); pola benar = verbatim anchored insertion dari WIP (import, full hook func, exclusion, call, content branch, anchor unique assert count==1); verifikasi akhir wave (16 selesai) = diff main:App.tsx vs WIP kosong
- 2026-09-04 Task14 OutboundForm PR slice — lazy per-task App patch (diff branch-base vs WIP, whole-hunk ownership + line-split mixed only, removals→context unless owner, recount headers); FEFO inline atomic (expiring-first, null-fallback, Stok tidak cukup rollback), transaksi keluar harga_jual_snapshot=harga_normal; route /keluar|/outbound|/sku/keluar|/sku/:id/keluar|?skuId
